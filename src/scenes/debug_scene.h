#pragma once

// Initializes the debug scene, loading resources and setting up the initial state
void InitDebugScene();

// Updates the debug scene, processing input and updating game objects
void UpdateDebugScene(float deltaTime);

// Renders the debug scene, drawing all game objects and UI elements
void RenderDebugScene();
